<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LotusV3.Views.ListView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="ListView Jugadores" HorizontalOptions="Center" FontSize="Title" TextColor="Black"/>
            <ListView x:Name="jugadores">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal">
                                <Image Source="{Binding Imagen}" WidthRequest="200" HeightRequest="100"/>
                                <StackLayout>
                                    <Label Text="{Binding Nombre}" FontSize="13" TextColor="DarkOrange" HorizontalOptions="FillAndExpand"/>
                                    <Label Text="{Binding Equipo}" FontSize="11" TextColor="Green" HorizontalOptions="FillAndExpand"/>
                                </StackLayout>
                                </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>